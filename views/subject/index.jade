ul#subjects
	- each subject in subjects
		- if (!subject.archived)
			li.subject(rel: subject._id)
				span.name #{subject.name}
				span.stars(href: '/' + subject._id, id: 'rating-' + subject._id, rel: subject.rating)
				.clear

form#newsubject(method: 'post', action: '/create')
	input(type: 'text', name: 'subject[name]')
	input(type: 'submit', value: 'Create New')
